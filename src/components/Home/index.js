import React from 'react'

const Home = () => {
    return <div>
        <span>React SSR Components</span>
        <button onClick={() => alert('hello React SSR')}>click</button>
    </div>
}

module.exports = Home